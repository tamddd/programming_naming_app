プログラミングの関数の、命名に関するアプリを作る。
関数を表示して、それにいい感じのネーミングをつけたい
そしてそれを評価する。
入力はどうしようかな？自由入力？まずは、簡単に候補を表示してチェックボックス？
か何かで、選択してもらおうか。


AI評価機能の実装案 (将来):
1. ユーザーが入力した「関数名」と「問題のコード」を受け取る
2. GoからGemini API (またはOpenAI API) を叩く
   - プロンプト例:
     "以下の関数コードに対して、ユーザーが考えた関数名 '{{user_input}}' を評価してください。
      コード: {{code}}
      評価基準: 命名規則、意図の明確さ、簡潔さ。
      出力形式: JSON { 'score': 0-100, 'feedback': '良い点と改善点' }"
3. 返ってきたJSONをパースして、フロントエンドに表示する
